FROM openjdk:8-jre-alpine

COPY ./jade.jar /

COPY ./tutorial3zatia /tutorial3zatia

ENV AMS_NAME=jade-ams

ENV SERVER_NAME=server

CMD ["java", "-cp", "/jade.jar:.", "jade.Boot", "-host", "${AMS_NAME}", "-port", "1099", "-container", "cliente:serverExample.Cliente(3)"]