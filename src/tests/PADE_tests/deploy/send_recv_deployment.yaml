apiVersion: apps/v1
kind: Deployment
metadata:
  name: pade-agent-send-recv
  labels:
    app: pade-agent-send-recv
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pade-agent-send-recv
  template:
    metadata:
      labels:
        app: pade-agent-send-recv
    spec:
      containers:
        - name: pade-agent-send-recv
          image: ekhurtado/agent-tests:pade-agent-send-recv
          imagePullPolicy: Always

      restartPolicy: Always
      serviceAccountName: ""

